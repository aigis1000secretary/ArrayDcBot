{
  "name": "arraydcbot",
  "version": "1.0.0",
  "engines": {
    "node": "16.x"
  },
  "description": "arraydcbot",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "npm init -y && npm i --save-dev node@16 && npm config set prefix=$(pwd)/node_modules/node && export PATH=$(pwd)/node_modules/node/bin:$PATH && exit 0"
  },
  "author": "K.T.",
  "license": "ISC",
  "dependencies": {
    "cheerio": "^1.0.0-rc.10",
    "discord.js": "^13.6.0",
    "dotenv": "^10.0.0",
    "express": "^4.17.2",
    "pg": "^8.7.1",
    "request": "^2.88.2"
  }
}
